server {
  listen 80;

  location / {
    root /usr/share/nginx/html;
    index index.html;
    try_files $uri /index.html;
  }

  location /api/auth {
    proxy_pass http://auth-service:5000/api/auth;
  }

  location /api/tasks {
    proxy_pass http://task-service:5001/api/tasks;
  }
}