apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: educollab-ingress
spec:
  rules:
    - host: educollab.local
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: frontend
                port:
                  number: 80